using Common.Model;

namespace BL.Interface
{
    public interface ISaleOrderBL : IBaseBL<SaleOrder>
    {
        // K? th?a t?t c? CRUD operations t? IBaseBL<SaleOrder>
        // GetAllAsync(), GetByIdAsync(), AddAsync(), UpdateAsync(), DeleteAsync(), DeleteMultipleAsync()
        // Thêm các method ??c thù cho SaleOrder n?u c?n
    }
}
